import React from "react";

import ContentView from "../../layouts/service-bus/forgot-password";

export const ForgotPasswordScreen = ({ navigation }): React.ReactElement => (
	<ContentView navigation={navigation} />
);
