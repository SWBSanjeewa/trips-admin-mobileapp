import React from "react";

import ContentView from "../../layouts/service-bus/sign-in";

export const SignInScreen = ({ navigation }): React.ReactElement => (
	<ContentView navigation={navigation} />
);
