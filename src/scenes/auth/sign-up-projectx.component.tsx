import React from "react";

import ContentView from "../../layouts/auth/sign-up-projectx";

export const SignUpProjectxScreen = ({ navigation }): React.ReactElement => (
	<ContentView navigation={navigation} />
);
