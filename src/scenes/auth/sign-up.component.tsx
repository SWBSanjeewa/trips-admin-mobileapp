import React from "react";

import ContentView from "../../layouts/service-bus/sign-up";

export const SignUpScreen = ({ navigation }): React.ReactElement => (
	<ContentView navigation={navigation} />
);
