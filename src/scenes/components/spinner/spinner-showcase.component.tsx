import { Spinner, SpinnerElement, SpinnerProps } from "@ui-kitten/components";
import React from "react";

export const SpinnerShowcase = (props?: SpinnerProps): SpinnerElement => <Spinner {...props} />;
