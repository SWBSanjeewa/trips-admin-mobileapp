import React from "react";

import { BusJourneyCard } from "./extra/busjourney-card.component";

export default ({ navigation }): React.ReactElement => {

	return (
		<BusJourneyCard navigation={navigation}/>
	);
};
