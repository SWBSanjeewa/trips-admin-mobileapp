import React from "react";
import UserprofileComponent from "./extra/userprofile.component";

export default ({ navigation }): React.ReactElement => {

	return (
		<UserprofileComponent navigation={navigation}/>
	);
};