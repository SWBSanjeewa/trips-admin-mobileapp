import React from "react";

import { BusJourneyLiveCard } from "./extra/busjourney-card.component";

export default ({ navigation }): React.ReactElement => {

	return (
		<BusJourneyLiveCard navigation={navigation}/>
	);
};
