import React from "react";

import VehiclePhotosAddCard from "./extra/vehiclephotos-add-card.component";

export default ({ navigation }): React.ReactElement => {

	return (
		<VehiclePhotosAddCard navigation={navigation}/>
	);
};
