import React from "react";
import BusLiveStoppings from "./extra/bus-live-stoppings.component";

export default ({ navigation }): React.ReactElement => {

	return (
		<BusLiveStoppings navigation={navigation}/>
	);
};