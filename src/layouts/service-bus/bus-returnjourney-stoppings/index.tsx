import React from "react";

import { BusReturnJourneyCard } from "./extra/busjourney-card.component";

export default ({ navigation }): React.ReactElement => {

	return (
		<BusReturnJourneyCard navigation={navigation}/>
	);
};
