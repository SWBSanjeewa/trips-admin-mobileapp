import React from "react";
import SigninComponent from "./extra/signin.component";

export default ({ navigation }): React.ReactElement => {

	return (
		<SigninComponent navigation={navigation}/>
	);
};