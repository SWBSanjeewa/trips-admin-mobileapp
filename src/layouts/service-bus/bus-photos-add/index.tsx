import React from "react";

import BusPhotosAddCard from "./extra/busphotos-add-card.component";

export default ({ navigation }): React.ReactElement => {

	return (
		<BusPhotosAddCard navigation={navigation}/>
	);
};
