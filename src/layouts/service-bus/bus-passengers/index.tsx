import React from "react";

import { BusPassengerCard } from "./extra/buspassenger-card.component";

export default ({ navigation }): React.ReactElement => {

	return (
		<BusPassengerCard navigation={navigation}/>
	);
};
