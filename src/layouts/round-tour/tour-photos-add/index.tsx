import React from "react";

import TourPhotosAddCard from "./extra/tourphotos-add-card.component";

export default ({ navigation }): React.ReactElement => {

	return (
		<TourPhotosAddCard navigation={navigation}/>
	);
};
